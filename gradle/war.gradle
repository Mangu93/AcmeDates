apply plugin: "war"

bootWar {
    mainClassName = "com.amp.acmedates.AcmeDatesApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
